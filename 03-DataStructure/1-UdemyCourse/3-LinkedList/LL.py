# Head => The first Node 
# Tail => the last item
# tykono les nodes mfr9in f memory  mais tykono m linkin dakchi 3lach mtnkhsrouch data 


###########################################################################################

"""
Big O in LL: 
- append(node) && append_node_in_head(node) => O(1)
- remove(node) => O(n)

"""
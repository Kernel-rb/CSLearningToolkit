# Set est une list avec {} sans doublant et fait partie de table de hashage
# on peut pas l indexer [i]
# set en algorithme => supprimer les doublants
set_names =  {"kernel" , "saif" }
for s in set_names :
    print(s)
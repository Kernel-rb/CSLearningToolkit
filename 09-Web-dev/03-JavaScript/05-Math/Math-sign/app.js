// Methode Math.sign() retourne 1 si le nombre est positif, -1 si le nombre est négatif et 0 si le nombre est 0.

console.log(Math.sign(1.2)); // 1
console.log(Math.sign(1.5)); // 1
console.log(Math.sign(1.8)); // 1
console.log(Math.sign(-1.2)); // -1
console.log(Math.sign(-1.5)); // -1
console.log(Math.sign(-2.8)); // -1
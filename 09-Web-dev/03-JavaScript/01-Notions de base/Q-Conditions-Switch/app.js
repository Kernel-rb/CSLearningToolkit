let color = "red"; // change the value to test the code
// switch est une alternative à if else if else
switch (color) {
    case "red":
        console.log(`The color is ${color}`);
        break;
    case "blue":
        console.log(`The color is ${color}`);
        break;
    case "green":
        console.log(`The color is ${color}`);
        break;
    default:
        console.log(`The color is not available`);
        break;
}
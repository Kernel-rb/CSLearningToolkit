// fonctions pures est une fonction qui ne modifie pas les variables extérieures

function additionner(a, b) {
    return a + b;
}


let cont = 0;
let increaser = () => cont++; // machi pure hit ttmodifier la valeur dyal cont kola mra kayn appel lfonction

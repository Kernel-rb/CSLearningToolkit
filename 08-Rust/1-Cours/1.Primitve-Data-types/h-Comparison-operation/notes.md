# Equal signe  in Rust 
        - `==` : Equal signe
        - `!=` : Not equal signe
        - `>` : Greater than signe
        - `<` : Less than signe
        - `>=` : Greater than or equal signe
        - `<=` : Less than or equal signe
        
